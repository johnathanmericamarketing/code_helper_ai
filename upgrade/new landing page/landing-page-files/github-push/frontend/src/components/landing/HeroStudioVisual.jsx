import React from 'react';

// HeroStudioVisual — the actual animated demo is embedded in HeroSection.jsx
// This component is kept for import compatibility.
export const HeroStudioVisual = () => {
  return null;
};
