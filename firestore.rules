rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // TODO: Restrict to authenticated users once Firebase Auth is added.
      // For now, open access matches the original API-key-based model.
      allow read, write: if true;
    }
  }
}
