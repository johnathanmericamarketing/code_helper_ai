[build]
  base = "frontend"
  command = "npm install --include=dev && npm run build"
  publish = "build"

[build.environment]
  NODE_VERSION = "20"
  CI = "false"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
